<div class="max-w-md mx-auto px-12 py-9 bg-emerald-50 rounded text-emerald-900 shadow shadow-emerald-100">
  <h1 class="text-2xl font-semibold mb-4">Log in</h1>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field mb-4 flex flex-col justify-between">
      <%= f.label :email, class: "after:content-['*'] after:ml-0.5 after:text-emerald-500" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "albert.einstein@mail.com", class: "mt-1 rounded-md border-emerald-200 shadow-sm focus:border-emerald-300 focus:ring focus:ring-emerald-200 focus:ring-opacity-50" %>
    </div>

    <div class="field mb-4 flex flex-col justify-between">
      <%= f.label :password, class: "after:content-['*'] after:ml-0.5 after:text-emerald-500" %>
      <%= f.password_field :password, autocomplete: "current-password", placeholder: "********", class: "mt-1 rounded-md border-emerald-200 shadow-sm focus:border-emerald-300 focus:ring focus:ring-emerald-200 focus:ring-opacity-50" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="field mb-4 flex items-center">
        <%= f.check_box :remember_me, class: "mr-2 border border-emerald-200 rounded text-emerald-400 shadow-sm focus:border-emerald-200 focus:ring focus:ring-emerald-200 focus:ring-opacity-50" %>
        <%= f.label :remember_me %>
      </div>
    <% end %>

    <div class="actions">
      <%= f.submit "Log in", class: "mb-3 py-2 px-3 bg-emerald-500 hover:bg-emerald-400 text-white rounded transition duration-300" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
